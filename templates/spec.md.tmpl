# Task: <one-line title>

## Context

<1-3 sentences explaining why this task exists, what problem it solves,
and what success looks like to a user. The fresh verifier will read this
without any other context — make it self-contained.>

## Acceptance Criteria

- AC1: <criterion that can be verified by running a specific command or
  reading a specific file — phrase it so the verifier knows exactly how
  to check>
- AC2: <criterion>
- AC3: <criterion>

## Out of Scope

- <Things this task explicitly does NOT cover, so the verifier does not
  flag them as missing>

## Verification Hints

- AC1 is verified by: `<command or check>`
- AC2 is verified by: `<command or check>`
- AC3 is verified by: `<command or check>`
